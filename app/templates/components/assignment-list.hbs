<table class="table table-hover assignment-list">
    <thead>
    <tr>
        <!--<th></th>-->
        <th class="date noselect pointer {{timestamp}} {{sortDirection}}" {{action 'sortBy' 'timestamp'}} >Due Date</th>
        <th class="time">Time</th>
        <th class="category noselect pointer {{assignment_name}} {{sortDirection}}" {{action 'sortBy' 'assignment_name' }}>Category</th>
        <th class="description noselect">Description </th>
        <th class="edit noselect stats">Stats </th>
        <th class="bulk noselect">
            {{#if selectionMade}}
                <button class="btn {{bulkButtonBackground}}" {{action 'bulkAction'}}>
                    {{bulkButton}}
                </button>
            {{else}}
                <button class="btn disabled">
                    {{bulkButton}}
                </button>
            {{/if}}
        </th>

    </tr>
    </thead>
    <tbody class="no-select">
        {{#each assignmentsSorted as |assignment|}}
            <tr id="{{assignment.id}}">
                <td {{action 'click' 'secure.course.assignment.edit' assignment.course_id.id assignment.id }}>
                    {{format-date assignment.dueDateMoment "dddd,"}} <br/>
                    {{format-date assignment.dueDateMoment "MMM Do"}}

                </td>
                <td {{action 'click' 'secure.course.assignment.edit' assignment.course_id.id assignment.id }}>
                    {{#if assignment.time_visible}}
                        {{format-date assignment.dueDateMoment "h:mm A"}}
                    {{else}}
                        -------------
                    {{/if}}
                </td>
                <td {{action 'click' 'secure.course.assignment.edit' assignment.course_id.id assignment.id}}>
                    {{assignment.assignment_name}}
                </td>
                <td class="description" {{action 'click' 'secure.course.assignment.edit' assignment.course_id.id assignment.id}}>
                    {{description-text assignment.description}}
                </td>
                <td class="fa fa-bar-chart stats" {{action 'click' 'secure.course.assignment.status' assignment.course_id.id assignment.id}}>
                </td>
                <td class="bulk">
                    <span{{action 'select' assignment}}>
                        {{#if assignment.selected}}
                            <i class="fa fa-check-square-o"></i>
                        {{else}}
                            <i class="fa fa-square-o text-muted"></i>
                        {{/if}}
                    </span>
                </td>
            </tr>
        {{/each}}
    </tbody>
</table>